serviceAccount: "your-service-account@your-project.iam.gserviceaccount.com"  # 서비스 계정 지정

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/${PROJECT_ID}/myimage:test', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/${PROJECT_ID}/myimage:test']

  - name: 'google/cloud-sdk'
    args:
      - 'gcloud'
      - 'run'
      - 'deploy'
      - 'pythonweb'
      - '--image=gcr.io/${PROJECT_ID}/myimage:test'
      - '--port=5000'
      - '--region=asia-east1'
      - '--platform=managed'
      - '--allow-unauthenticated'

options:
  logging: "CLOUD_LOGGING_ONLY"
